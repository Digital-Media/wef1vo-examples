export default [
    {
        rules: {
            "semi": "error", // Error on missing semicolon
            "indent": "warn", // Warning on inconsistent indentation
            "prefer-const": "error" // Error when let is used instead of const
        }
    }
];
